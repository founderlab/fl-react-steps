{
  "name": "fl-react-steps",
  "version": "0.1.1",
  "description": "Step header and content components with which to make a multi step form",
  "main": "lib/index.js",
  "author": {
    "name": "Gwilym Humphreys",
    "url": "https://github.com/gwilymhumphreys"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/founderlab/fl-react-steps"
  },
  "scripts": {
    "prepublish": "rm -rf ./lib && babel ./src --ignore '/node_modules/' --out-dir ./lib",
    "build": "rm -rf ./lib && babel ./src --ignore '/node_modules/' --out-dir ./lib",
    "watch": "rm -rf ./lib && babel ./src --ignore '/node_modules/' --watch --out-dir ./lib",
    "test": "eval $(cat test/.env) mocha test/**/*.tests.js"
  },
  "dependencies": {
    "classnames": "^2.2.3"
  },
  "devDependencies": {
    "babel": "^5.6.14",
    "babel-core": "^5.6.15",
    "babel-eslint": "^4.1.3",
    "eslint": "^1.5.1",
    "eslint-config-founderlab": "^0.1.0",
    "eslint-plugin-react": "^3.4.2",
    "mocha": "*",
    "expect": "^1.12.2"
  }
}
